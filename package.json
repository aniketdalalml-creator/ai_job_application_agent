{
  "name": "ai-job-application-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-agent AI job application pipeline with Claude API, Express, and React",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix frontend",
    "dev:server": "npm run dev --prefix server",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "start": "npm run build:frontend && npm start --prefix server"
  }
}
